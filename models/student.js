class Student {
  constructor(id, firstName, lastName, phoneNumber, subject, status, year) {
    this.id = id,
    this.firstName = firstName,
    this.lastName = lastName,
    this.phoneNumber = phoneNumber,
    this.subject = subject,
    this.status = status,
    this.year = year
  }
}

module.exports = Student